# 🔴 세션 시작 규칙 (오너 지시, 2026-08-12)

**1. 작업 시작 전 [STATUS.md](STATUS.md)를 먼저 읽는다.**
현재 상태·오너 대기 항목·이미 종결된 것·내 환경 제약이 한 장에 있다.
**이미 답이 난 것을 다시 조사하지 않기 위한 문서다.**

**2. 작업이 끝날 때마다 두 곳을 갱신한다.**
```
STATUS.md    현재 상태가 바뀌었으면 갱신(한 장 유지 — 길어지면 WORKLOG로 밀어낸다)
WORKLOG.md   무엇 / 왜 / 결과 / 검증 / 관련  형식으로 append(상세 이력)
```
**3. 세션이 바뀌어도 이어지도록 남긴다.** 대화는 사라져도 파일은 남는다.
**"기억"에 의존하지 말고 파일에 적는다.**

**4. 기록은 파일에, 보고는 채팅으로. 둘 다 한다(오너 지시 2026-08-12).**
```
파일   STATUS.md · WORKLOG.md · 건별 문서   → 남기기 위한 것(다음 세션·다음 사람)
채팅   CTO/웹/콘텐츠/디자인 세션에 send_message → 전달하기 위한 것(지금 그 사람)
```
- **파일만 쓰면 상대가 못 본다. 채팅만 하면 세션이 끝나면 사라진다.** 그래서 둘 다다.
- 채팅 보고는 **한 줄 요약 + 근거(무엇으로 확인했는지) + 상대가 할 일**을 담는다.
- 파일에는 **판단 근거와 재검토 조건**까지 남긴다(같은 조사를 다시 하지 않도록).

---

# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

---

# 검증 규칙 (오너 지시, 2026-08-10)

> 하루에 같은 종류의 오판을 7번 냈다. **몰라서가 아니라 "확인했다고 믿고" 틀렸다.**
> 7건 전부 확인 수단이 한 칸 옆에 있었다. 아래는 그 7건에서 그대로 뽑은 규칙이다.

## 1. 모든 주장에 근거를 붙인다

보고에 단정을 쓸 때는 **어떻게 확인했는지**를 함께 적는다.

```
주장: 시드 지점 150건이 공개 노출 중
근거: curl로 /map 받아서 회사명 grep 매치
```

**근거를 쓰는 순간 틀린 게 보인다** — grep 매치는 화면이 아니다.
근거를 못 쓰겠으면 **확인하지 않은 것**이므로 보고하지 않는다.

## 2. "보인다/안 보인다"는 브라우저로 확인한다

`curl`·`grep`은 **존재 확인용**이지 **노출 확인용이 아니다.**

- 문자열이 HTML에 있다 ≠ 사용자에게 그 의미로 보인다
  (실제 사고: `/map`에 걸린 회사명은 지점이 아니라 **GA 필터 드롭다운 목록**이었다)
- `HTTP 200` ≠ 페이지가 열린다
  (실제 사고: Next.js는 **not-found 페이지도 200**으로 내려준다)

사용자에게 무엇이 보이는지 말하려면 **브라우저로 그 화면을 연다.**

## 3. DB는 한 컬럼만 보고 판단하지 않는다

`status='visible'`을 보고 "노출 중"이라 단정하지 않는다.
**소프트 삭제(`deleted_at`)·만료·권한 등 같은 테이블의 다른 조건**을 함께 본다.

> 실제 사고: 153건 전부 `deleted_at`이 찍혀 있었는데 `status`만 보고
> "가짜 지점이 공개돼 있다"고 오너에게 경보를 올렸다. 오경보였다.

**0 데이터 구간에서는 성공과 실패가 같은 모양이다.** 화면이 비어 있는 것은
"데이터가 없다"일 수도 "기능이 없다"일 수도 있다. 구분하려면
`pg_proc`·`pg_constraint`처럼 **존재 자체를 묻는 쿼리**를 쓴다.

## 4. 부분 문자열로 확인하지 않는다

`grep -q "휴대폰 번호"`는 **다른 행에 있는 같은 문자열**에 걸린다.
`pg_get_functiondef`는 **주석까지 포함**하므로 내가 쓴 주석에 내 패턴이 매치된다.

확인은 **전체 문자열**이나 **구조를 특정하는 패턴**으로 한다.

## 5. 팀 메시지는 발신자 ID로 귀속한다

본문의 자기지칭("이 모바일 세션에서…")이 아니라 **메시지 헤더의 세션 ID**를 본다.

> 실제 사고: 웹 세션이 보낸 보고를 본문만 보고 앱 세션 작업으로 귀속해,
> 아무 상관 없는 세션에 지시를 보냈다. 발신자 ID는 헤더에 그대로 있었다.

## 6. 오너의 표현을 플랫폼 용어로 직역하지 않는다

**"무엇을 하고 싶은가"를 먼저 확인한다.** 되묻는 비용은 몇 초, 틀리는 비용은 반나절이다.

> 실제 사고 2건:
> - *"친구메세지 심사 신청해둬라"* → 카카오 콘솔의 `talk_message`로 직역.
>   실제로 원한 것은 **카카오톡 공유(Kakao Share)** 였고 **심사 자체가 불필요**했다.
> - *"어플공유링크를 친구한테"* → 지점 상세 페이지 공유로 해석.
>   실제로 원한 것은 **앱/사이트 자체를 보내는 것**이었다.

## 7. 같은 화면을 두 번 보면 어느 단계인지 확인한다

> 실제 사고: 카카오 "비즈니스 정보" 화면을 두 번 봤는데, **앞 단계(비즈 앱 전환)의
> 첨부 요구조건을 뒤 단계(추가 기능 신청)의 것으로 옮겨 적었다.**
> 실제로는 첨부 없이 즉시 승인됐다. 오너를 반나절 대기시켰다.

## 8. "이미 처리됐는지" 먼저 본다

지시를 내린 뒤 상태를 보고할 때는 **현재 상태를 확인**하고 말한다.

> 실제 사고: 웹팀이 이미 커밋·배포까지 끝낸 P0를 확인 없이
> "웹팀 작업 대기 중"이라고 오너에게 보고했다. `git log` 한 줄이면 됐다.

## 9. 검증할 수 없으면 배포하지 않는다

"정직하게 한계를 명시했다"는 면죄부가 아니다.
**검증 불가는 "나중에 검증한다"가 아니라 "지금 내보내지 않는다"는 뜻이다.**

## 10. 없는 기능으로 권한을 신청하거나 약속하지 않는다

심사 반려·허위 신고는 기록에 남아 다음 신청을 불리하게 만든다.
체크박스에 사실이 아닌 것을 체크하지 않는다.
