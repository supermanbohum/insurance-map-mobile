# 빌드 스모크 체크리스트 (versionCode 6, 실기기 실행용)

> 작성: 2026-08-07 · 대상: 이번 EAS 프로덕션 빌드(vc5→6). **디바이스 보유자(오너)가 실기기에서 실행.**
> 앱팀은 이 환경에 JDK/SDK/에뮬레이터가 없어 직접 실행 불가 → 결과(OK/NG·증상·result)를 받아 진단·수정한다.
> ⚠️ **카카오 로그인은 이 빌드에 없다**(웹 미배포). 카카오 검증은 [KAKAO_LOGIN_RESEARCH.md](KAKAO_LOGIN_RESEARCH.md) §A-009(웹 배포 후) 참조.

## 이번 빌드에 실린 것
딥링크 실경로 정합화(A-002) · Google 분기 비활성/OAuth 인프라 보존(A-001) · V2 스플래시/아이콘 · 오프라인/에러 안정성 · **FCM 배선(A-010)**.

## 설치
- [ ] 기존 Play 알파(vc5)가 깔려 있으면 **삭제 후 설치**(preview APK는 서명 달라 덮어쓰기 안 될 수 있음).

## 브랜드 (V2)
- [ ] **스플래시**: 밝은 블루 그라데이션 → **로고 등장 → "대한민국 보험인의 지도 / 70만 보험인의 선택"(함께) → "보험맵"** 순. **원형 glow·이상한 배경 없음.**
- [ ] **아이콘**: 홈 화면에 **파란 아이콘(흰 한반도)**, 검정 테두리 없음. iOS는 모서리 자동 라운딩.

## 딥링크 (A-002) — 카톡/브라우저에서 링크 탭 or `adb`로 스킴 실행
- [ ] `boheommap://branch/{실제slug}` → 앱 열리고 해당 지점.
- [ ] 구경로 `boheommap://designer/1` → **/planner-market/1**로 이동(매핑).
- [ ] `boheommap://chat/anything` → **/chat**(단일 룸).
- [ ] `boheommap://notice/5` → **/board/notice**.
- [ ] 미지 `boheommap://zzz/1` → **홈**(빈 화면/404 아님).
- [ ] 한글 slug 지점도 정상 진입(퍼센트 인코딩).

## 네이티브 기능
- [ ] **공유**: 웹에서 프로필/지점 공유 → 네이티브 공유 시트(카톡 등).
- [ ] **생체인증 잠금**: 설정 on → 앱 백그라운드→복귀 시 잠금 화면 + 지문/Face로 해제.
- [ ] **QR 스캐너**: 웹의 QR 스캔 트리거 → 카메라 열림 → 코드 인식 → 값 반영. 닫기 정상.
- [ ] **다운로드**(Android): 웹에서 파일 다운로드 → 갤러리 저장 Alert.

## 안정성/UX
- [ ] **오프라인**: 비행기모드 → 오프라인 오버레이. **작성 중 폼 유지**. 복귀 시 오버레이 자동 사라짐(콜드스타트 오프라인만 자동 리로드).
- [ ] **에러화면**: 로드 실패 시 "페이지를 불러오지 못했습니다" + 다시 시도. **정상 페이지를 덮지 않음**(idle 서브리소스 오류로 안 뜸).
- [ ] **로딩바**: 페이지 이동 시 상단 진행바 → 완료 시 사라짐.
- [ ] **Safe-area**(Android edge-to-edge): 웹 헤더/하단탭이 상태바·제스처바에 안 겹침.
- [ ] **iOS pull-to-refresh**: 상단 당김 새로고침. **Android는 없음(의도된 정상 — 스크롤 충돌 방지)**.
- [ ] **뒤로가기**(Android): 웹 히스토리 뒤로 → 최상단에서 "한 번 더 누르면 종료" 토스트+햅틱.

## FCM 푸시 (A-010, 서비스계정 키 등록됨)
- [ ] 앱 첫 실행 → **알림 권한 허용** → Expo 푸시 토큰 획득(로그/토스트로 확인) → 브릿지로 웹 전달 → **웹 register_push_token 저장 확인**.
- [ ] Expo Push 도구/서버로 **테스트 푸시 발송** → 알림 수신.
- [ ] 알림 **탭** → `data.path`로 딥링크 진입(예: `/chat`, `/planner-market/{id}`).
- [ ] (선택) `set-badge` → 앱 아이콘 뱃지 수 표시.

## 기록 양식
| 항목 | OK/NG | 증상·비고 |
|---|---|---|
| … | | |

> NG가 나오면 증상(+가능하면 result.type/콘솔 로그)을 앱팀에 전달 → 진단·수정. 특히 딥링크 폴백, 오프라인 폼 유지, FCM 토큰 전달을 우선 확인.
