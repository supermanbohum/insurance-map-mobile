# 앱 성장 기획 응답 (A-005 / A-006)

> 작성: 2026-08-07 · 지시: CTO A-005(앱 사용률 합동기획, 앱팀 중심)·A-006(빈 상태 화면 점검) · 브리프: `C:\Dev\boheommap-cto\APP_GROWTH_BRIEF.md`
> 전제: 정식 출시 전(지점 1·사용자 5), 인플루언서 1회 피크 대비, FCM 키 미등록(코드 완료), 인스타/메타 광고, 타겟=지점장·본부장·단장(개인사업자). 카카오 로그인 확정.
> 방법: 실기기 없음 → 앱 WebView가 로드하는 운영 사이트를 **브라우저 모바일 뷰포트(375×812)로 실측**. app-only 상호작용은 "미확인(실기기 필요)"로 표기.

---

## §6 CTO 가설에 대한 입장 — **동의(강함)**
> 가설: "앱의 진짜 가치는 푸시+딥링크뿐, 나머지는 부차적. '알림 받는 도구'로 포지셔닝하라."

- **동의 근거**: 앱은 `bohummap.com` WebView 셸이라 화면이 웹과 **동일**(실측 확인). "더 빠르다/편하다"는 구조적으로 성립 불가 → 그 방향 투자는 낭비. 앱의 유일한 구조적 우위 = **OS 도달(푸시) + 진입(딥링크) + 기기기능(생체/QR/공유)**. 타겟(지점장)의 "내 지점 조회·문의를 즉시 알고 싶다"는 니즈는 **푸시만** 충족.
- **보완(반박 아님)**: 생체인증 앱잠금(고객 민감정보 보호)은 개인사업자에게 **두 번째 설치 이유**가 될 수 있음(0은 아님). 단 우선순위는 푸시+딥링크가 명확히 1순위.

---

## A-005 답변

### Q1. 지금 자산 조합으로 "앱에서만 되는 것" 몇 개? (신규 개발 0)
보유 자산: 푸시·딥링크·공유·생체인증·QR·뱃지 (전부 구현 완료).
- **① 지금 가능(조합만, 1~2주 내)**
  1. **푸시 → 딥링크**: 알림 탭 시 해당 지점/프로필/글로 직행 (핵심 루프)
  2. **공유 → 딥링크**: 내 지점/프로필 카톡 공유 → 링크 탭 시 앱으로(설치자)
  3. **QR → 딥링크**: 대면에서 명함 QR 스캔 → 상대 프로필 열기
  4. **생체인증 앱잠금**: 고객정보 보호 토글
  5. **뱃지**: 미읽음 알림 수 표시(재방문 유인)
  → **5개 즉시 가능.** 단 1·2·5는 **푸시(FCM 키)** 와 딥링크가 살아야 의미. 딥링크는 완료(A-002), 푸시는 FCM 키만 남음.
- **② 투자 필요**: 로컬 알림 기반 재방문 넛지(문구/타이밍 설계 필요) — 가치 중.
- **③ 하지 말 것**: "앱이 더 빠름" 류 네이티브 화면 재구현 — WebView 구조상 무의미.

### Q2. 푸시 알림 종류 (기술 분류)
- **웹이 이벤트를 줘야 발송 가능(웹 백엔드 트리거 + Expo Push)**:
  · **문의 도착**(비로그인 문의폼 W-059 → 새 소재) · **내 지점/프로필 조회** · 채용 지원/제안 · 승인 완료(지점 등록/배지) · 새 채팅 메시지 · 새 커뮤니티 글/댓글 · 연봉랭킹/TOP설계사 관련
  · **웹팀 요구사항 2개**: (a) `push-token` 수신→`register_push_token` 저장(계약 완료) (b) **이벤트별 Expo Push 발송 트리거**(우선: 문의도착·프로필조회 — §6 가설과 정확히 일치)
- **앱 단독 가능(웹 불필요)**: 로컬 예약 알림(재방문 넛지 "N일 미접속", 온보딩 후 팁), 뱃지 수 갱신. `expo-notifications`(기설치)로 신규 의존성 0. 단 소재가 빈약해 보조 수단.

### Q3. WebView에서 어색한 화면? (실측 + 미확인)
- **실측(브라우저 모바일뷰, = 앱 WebView 렌더)**: 홈 / 커뮤니티(목록·상세) / 지점찾기(`/ga`) / 마이(`/my` 로그인폼) **모두 정상 렌더, 홈 콘솔 에러 0.** 빈 상태도 graceful(지점찾기="검색/필터 유도", 마이=로그인폼). 지점 1개가 인기·신규 양쪽 노출(빈약하나 안 깨짐).
- **알려진 웹 결함(앱에도 동일, 웹 수정 대상)**: W-058 마크다운 raw(글 상세 — 이번 샘플 글엔 미노출이나 마크다운 글에서 발생), W-054 홈 통계 SSR0/client103 → **앱 WebView는 클라이언트 렌더라 숫자(403 등)가 보임 = 앱에선 오히려 정상**.
- **미확인(실기기 필요, app-only 상호작용)**: ① safe-area inset ↔ 웹 고정 헤더/하단탭 겹침 ② iOS pull-to-refresh 제스처 ③ 파일 업로드/다운로드 ④ **카카오톡 앱전환→WebView 복귀**(A-004 핵심). → 다음 빌드 실기기 스모크에서 확인 권장.
- **결론**: **앱 셸 고유의 빈-상태 붕괴는 관측되지 않음.** 빈 상태 시각/무한스크롤/마크다운은 전부 웹(CSS/렌더러) 소관 → 웹 수정 시 앱 자동 반영.

### Q4. 웹 불가 영역 중 현실적인 것 (Expo57 난이도/신규 의존성)
- **① 지금 가능**: **로컬 알림 재방문 넛지** — `expo-notifications`(기설치), 신규 의존성 0, 난이도 하.
- **② 투자 필요(가치<비용, 현 단계 보류)**: 홈화면 바로가기(App Shortcuts/Quick Actions) — Android 동적 shortcut은 신규 라이브러리 가능성, 난이도 중, 셸 앱이라 가치 낮음.
- **③ 하지 말 것(현 단계)**: **홈 위젯**(네이티브 위젯 신규 개발, SDK57 부담 큼) · **오프라인 캐시**(WebView 오프라인=웹 서비스워커 영역, 앱단은 큰 투자에 데이터도 적음). 둘 다 사용자 5명 단계에서 역효과.

---

## "앱 사용률을 올리는 단 하나" — **문의 도착 + 내 지점 조회 푸시(→딥링크)**
- **무엇**: 웹이 (a) 지점 문의 도착, (b) 내 지점/프로필 조회 이벤트 발생 시 Expo Push 발송 → 알림 탭 시 딥링크로 해당 지점/프로필 직행.
- **근거**: §6 가설과 정확히 일치. 지점장(타겟)의 **즉시성 니즈**를 앱만 충족. 이미 딥링크(A-002)·토큰 전달·뱃지 완료 → **남은 건 FCM 키 등록(오너) + 웹 발송 트리거 2개뿐.** 인플루언서 피크일에 "설치하면 내 지점 반응을 즉시 안다"가 **유일하게 설득력 있는 설치 이유**.
- **하지 말 것**: 그 외 앱 전용 기능에 개발 리소스 분산.

---

## A-006 빈 상태 화면 점검 결과 (요약)
| 화면 | 실측 결과 |
|---|---|
| 홈 `/` | 정상. 방문자 수 표시(클라 렌더), 지점 1개가 인기/신규 노출. 콘솔 에러 0 |
| 커뮤니티 `/community` | 목록 5글 정상. 상세 `/post/[id]` 렌더 정상(샘플 글 raw 마크다운 없음) |
| 지점찾기 `/ga` | 빈 상태=검색/필터 유도 문구(정상) |
| 마이 `/my` | 로그인 폼(아이디/비번찾기 포함). 소셜 버튼 없음(Google 제거·카카오 미구현 확인) |
- **앱 셸 고유 붕괴 없음.** app-only 상호작용(safe-area/pull-to-refresh/업로드/카톡전환)은 실기기 미확인 → 다음 빌드 스모크에서 확인.

---

## A-002 상태 (한 줄)
✅ **완료·푸시(fe06cfc)** — 딥링크가 실제 웹 라우트와 정합화됨(구경로 매핑 + 미지/스텁 홈 폴백, 404 방지). 푸시가 살아나면 즉시 사용 가능.

> 결론: **FCM 키 등록(오너) + 웹 발송 트리거 2개(문의·조회)** 가 앱 사용률의 단일 최대 레버. 앱측 선행 작업(딥링크·토큰·뱃지)은 완료 상태.

---

## 추가(2026-08-07 후속): 빌드 타이밍 권고 + 정정

### 카카오를 다음 빌드에 "포함"해야 하나? → **아니오. 지금 바로 빌드 권장(카카오 대기 불필요).**
- **근거**: 권고안(옵션 ①)의 카카오 로그인은 **앱 코드 변경이 사실상 0**이다. OAuth 우회 인프라(Custom Tab+스킴 콜백)가 A-001로 이미 현재 코드에 살아있으므로, **웹이 Supabase 카카오 provider + 로그인 버튼만 켜면 이미 배포된 앱에서 그대로 동작**한다.
- 즉 카카오 때문에 빌드를 미루면 A-002·V2(스플래시/아이콘)·안정성·푸시딥링크 준비분만 늦어져 **손해**. **① 지금 빌드 1회**(현 작업 일괄) → 웹 카카오는 그 빌드에서 자동 지원. **별도 카카오 빌드 불필요.**
- **예외**: 카카오톡 "원탭 앱-투-앱" UX를 위해 **옵션 ③(네이티브 SDK)** 을 택하면 그때만 추가 빌드 1회 필요(신규 의존성 사전 보고 대상). 그 전까지는 옵션 ①로 충분.
- **검증**: 카카오톡 앱전환→WebView 복귀는 빌드 블로커가 아니라 **실기기 스모크 항목**. 불안정 시 폴백=카카오 계정 웹로그인(빌드 무관).

### 정정(웹 최신 반영)
- **W-054(홈 통계 SSR/클라 불일치) 해소** — SSR 통합 완료. 앱 WebView에서도 동일 값. (본문 Q3의 W-054 언급은 해소됨.)
- **W-058(마크다운 raw) 해소** — 커뮤니티 공식 글 마크다운 렌더링 정상 확인. (앱 WebView도 동일.)
- **W-059 문의 폼**: 비로그인 지점 문의 → "문의 1건 도착"이 조회보다 **강한 푸시 소재**. Q2 알림 목록 최우선.
- **W-063 설계사 가치 화면** 착수(웹) — 인플루언서 팔로워 다수가 설계사라는 판단. 앱은 WebView로 자동 반영, 코드 작업 불필요.
