# QA 소견 — 앱 셸 (품질조, 앱팀 몫)

> 담당: 모바일 앱(WebView 셸 고유 영역). 웹은 사용자 동선 전수를 별도 담당.
> 방법: ⚠️ 앱팀은 **실기기 테스트 불가**(빌드/디바이스 없음). 아래는 **① bohummap.com 모바일 뷰포트 직접 점검**(=앱 WebView가 렌더하는 것과 동일) + **② 셸 코드 리뷰**. 실기기 확인 필요 항목은 **[기기]**로 표시 → 8/10 테스터 12명 또는 오너 스모크로 검증.
> 형식: 발견 / 재현 / 심각도(P0~P3) / 담당 / 오픈전 필수. (앱 출시 일정 미정이라 대부분 "오픈전 필수=No", 단 8/10 테스터·오픈 시 모바일웹 빈상태는 관련)
> 최신화: 2026-08-08.

---

## A-006 — 0건(시드 삭제) 상태 화면 점검 (2026-08-11)

> 전제: ga_branch 153건 전량 soft-delete → **살아있는 지점 0건**(CTO DB 확인). 오픈 초기 사용자가 볼 화면.
> ⚠️ **방법과 한계**: 실기기 없음. **Browser pane(실제 Chromium 렌더링) 375×812**에서 점검 — curl/문자열 검색이 아니라 **렌더된 요소의 computed 높이·가시성·이미지 로드 상태 계측**. 단 **스크린샷은 pane 미표시로 생성 불가**(환경 제약), 데스크톱 Chromium이라 **실기기와 동일하지 않음**.
> **대조 검증**: 데이터가 있는 화면(/community)을 같이 열어 "0건"과 "깨짐"을 구분(CTO 지시).

| 화면 | 로더 잔존 | 렌더 높이 | 0건 처리 | 판정 |
|---|---|---|---|---|
| `/map` | 스피너 **0** | 컨테이너 **755px**, **지도 타일 8장 전부 실제 로드**(nrbe.pstatic.net, naturalWidth>0) | "지금 화면에 0곳 / 이 화면 범위에는 등록된 지점이 아직 없습니다" + **지점 등록하기** | ✅ |
| `/search` (초기) | 0 | main 376px | "지점명 또는 소속 회사명을 검색하거나 필터를 사용해보세요" (검색 유도) | ✅ |
| `/search?q=서울` (**0건 결과**) | 0 | main 450px | "'서울'에 대한 검색 결과가 아직 없습니다 / 우리 지점이 첫 번째가 될 수 있습니다" + **무료로 등록하기·지도에서 보기** | ✅ 우수 |
| `/region` | 0 | main 804px | 17개 시도 **전부 "1호 지점 등록"** 으로 전환 | ✅ 우수 |
| `/ga-ranking` | 0 | main 698px | "1위 자리가 비어 있습니다 / 첫 점수를 만드는 GA가 1위로 시작합니다" | ✅ 우수 |
| `/community` (**대조군, 데이터 有**) | 0 | main 994px, **글 9건 렌더** | — | ✅ 렌더 파이프라인 정상 |

**결론**: 4개 항목 판정
1. **지도 0마커** — 빈 지도 아님(타일 정상), 로더 안 멈춤 없음, 높이 붕괴 없음. ✅
2. **지점 목록 0건** — 흰 화면 아님. 전 화면이 안내문구 + 등록 CTA로 처리됨. ✅
3. **당겨서 새로고침** — **Android는 기능 자체가 없음**(App.tsx `pullToRefreshEnabled`는 react-native-webview **iOS 전용**, Android는 스크롤 충돌 이력으로 ScrollView 래핑 안 함). → "0건이라 안 바뀌는 걸 실패로 오인" 리스크는 **Android 해당 없음**. **iOS만 [기기] 확인 필요**.
4. **높이 붕괴** — 웹 콘텐츠 bodyScrollHeight 1216~1834 > 뷰포트 812(전 화면 콘텐츠 존재) + 셸의 `styles.webview flex:1`(콘텐츠와 무관하게 화면 채움) → **구조적으로 0 붕괴 불가**. ✅

**대조 검증 결과**: 데이터가 있으면(/community 글 9건) 리스트가 실제로 렌더됨 → 지점 화면의 빈 상태는 **"기능 깨짐"이 아니라 "데이터 없음"**으로 확정.

### A-006에서 새로 발견한 것 (기존 목록에 없던 것)
| # | 발견 | 재현 | 심각도 | 담당 | 오픈전 필수 |
|---|---|---|---|---|---|
| A8 | **`/map`에 푸터가 붙어 페이지가 세로로 스크롤됨**(bodyScrollHeight 1595 = 지도 755 + 푸터 ~840). 앱 WebView에서 **지도 팬(드래그)과 페이지 스크롤이 충돌**할 수 있음 — 지도를 위아래로 끌면 페이지가 스크롤될 여지. | 앱에서 /map 열고 지도를 세로로 드래그 | P2 | 앱→웹 | No(확인 후 판단) |
→ A8은 **[기기] 확인 필요**(데스크톱 마우스 드래그와 터치 제스처는 동작이 다름). 실제 충돌 시 웹에서 지도 영역 `touch-action` 처리 필요.

---

## ✅ 정상 확인 (모바일 웹 직접 점검)
| # | 항목 | 결과 |
|---|---|---|
| OK1 | 홈 빈 상태 | "아직 조회 데이터가 없습니다" / "신규 등록된 지점이 없습니다" + **"지점 등록하기" CTA** + "지역 1호 지점을 선점하세요" 배너 → 깨짐 없이 **기회 메시지로 처리됨** |
| OK2 | /map 빈 상태 | NAVER 지도 정상 렌더(© NAVER) + "이 화면 범위에는 등록된 지점이 없습니다 / 지금 화면에 0곳" · **콘솔 에러 0** |
| OK3 | /community | 목록·카테고리·글쓰기 정상, 글 6건 렌더 |
→ **오픈 시 사람이 보는 빈 화면은 앱/웹 모두 안전.** 빈 상태가 "고장"이 아니라 "1호 선점" 유도로 보임.

---

## 🔎 앱 셸 고유 — 실기기 확인 필요 (8/10 테스터/오너 스모크)
| # | 발견 | 재현 | 심각도 | 담당 | 8-17필수 |
|---|---|---|---|---|---|
| A1 | **[기기]** /map의 NAVER 지도 SDK가 **앱 WebView 안에서** 타일 렌더 + "내 위치"(geolocation) 동작하는지. 모바일 웹은 정상이나 앱 WebView는 미검증(geolocationEnabled 설정됨, 앱이 위치권한 요청). | 앱에서 지도 열기 → 타일 뜨나 + 내 위치 버튼 | P2 | 앱 | No(앱 오픈 후) |
| A2 | **[기기]** 푸시 수신→탭→목적지 도달. 콜드/웜. | PUSH_E2E_CHECKLIST A·B·C | P1 | 앱 | 테스터엔 관련 |
| A3 | **[기기]** 딥링크 콜드스타트 착지(앱 종료 상태에서 https/커스텀 링크). | 링크 탭→앱 열림→정확 화면 | P2 | 앱 | No |
| A4 | **[기기]** 안전영역 — 노치/제스처바에 웹 헤더·하단탭 안 가리는지(앱이 top/bottom inset 흰색 패딩). | 여러 화면 상/하단 확인 | P2 | 앱 | No |
| A5 | **[기기]** 뒤로가기(WebView 히스토리→2회 종료) + 스크롤(Android pull-to-refresh 충돌 없는지, 과거 버그). | 스크롤/뒤로가기 | P2 | 앱 | No |
| A6 | **[기기]** 스플래시→WebView 전환 매끄러움(흰 플래시·깜빡임 없이). vc9 오너 통과했으나 새 빌드 재확인. | 앱 콜드 실행 | P2 | 앱 | 테스터엔 관련 |

---

## 🧩 코드 리뷰 소견 (앱에서만 깨질 수 있는 것)
| # | 발견 | 심각도 | 조치 |
|---|---|---|---|
| C1 | **외부 도메인 이탈 규칙**: `shouldOpenExternally`가 bohummap.com/www 외 **모든 https 풀네비게이션을 외부 브라우저로 내보냄**. 지금은 안전(웹이 단일 도메인). ⚠️ 웹이 **다른 호스트(CDN·서브도메인)로 풀페이지 이동**을 넣으면 앱이 브라우저로 튕김. | P2(감시) | 웹이 도메인 추가 시 앱 `ALLOWED_WEB_HOSTS` 갱신 필요 → 웹팀 사전 공유 |
| C2 | **NAVER 지도 팝오버/외부링크**: 지도 SDK가 top-level 네비게이션을 naver 도메인으로 시도하면 C1 규칙에 걸려 브라우저로 나갈 수 있음(타일은 XHR/img라 무관). A1 기기확인 시 함께 관찰. | P2 | A1과 함께 확인 |
| C3 | **SPA 라우팅 확인**: /map·/community 이동 시 URL이 bohummap.com으로 rewrite = 웹은 SPA. 앱 딥링크는 `window.location.href` 풀네비라 **확실히 이동(무거우나 안전)**. 버그 아님. | — | 정보 |
| C4 | OAuth 우회 경로는 현재 미발화(소셜로그인 없음). 카카오 켜질 때 재검토(기존 감시항목). | — | 전제 변경 시 |

---

## 요약
- **빈 상태(오픈 핵심)는 앱/웹 모두 안전** — 직접 확인함.
- **앱 셸 고유 리스크는 대부분 실기기 검증 필요** → 8/10 테스터 12명 + 오너 스모크가 실질 QA. PUSH_E2E_CHECKLIST + BUILD_SMOKE_CHECKLIST가 그 도구.
- **코드 리뷰 P0 없음.** C1(도메인 이탈)만 "웹이 도메인 추가 시" 조건부 위험 → 웹팀과 사전 공유 필요.
- 앱 출시 일정 미정이라 **앱 항목의 "오픈전 필수"는 대부분 No**, 단 8/10 테스터가 받을 빌드 품질(A2·A6)은 지금 관련.
