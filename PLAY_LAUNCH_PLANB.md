# 🚀 Play 출시 플랜B — 오픈 대비 실행서

> 정식 오픈일 **미정**(웹 우선 오픈 방침). 앱은 프로덕션 가능하면 같이, 아니면 **내부/비공개 테스트로 운영하다 요건 충족 후 프로덕션 전환**.
> 이 문서는 **콘솔 결과가 나오기 전에 미리 준비**하는 것들. 마스터 제출 콘텐츠는 [PLAY_STORE_RELEASE.md](PLAY_STORE_RELEASE.md).

---

## 0. 왜 앱이 오픈을 못 막나 (판단 근거)
- 오픈 목적 = **공급 확보(지점·설계사 등록)**. 등록은 전부 **웹 폼**이고 모바일 웹에서 정상 동작.
- 앱은 **WebView 셸** = 같은 사이트(bohummap.com)를 감싼 것. 앱이 없어도 기능 손실 0.
- 앱 고유 가치 = **푸시(재방문)**인데, 이는 **사용자가 쌓인 뒤** 의미가 생김.
→ 오픈은 웹으로 먼저, 앱은 트랙 상황에 맞춰 붙인다.

---

## 1. 빌드 준비 상태 — ✅ 확인 완료 (2026-08-08)
| 항목 | 상태 |
|---|---|
| eas.json production | `distribution: store · buildType: app-bundle · autoIncrement: true` ✅ |
| app.json | version 1.0.0 · package com.bohummap.app · versionCode 6(프로덕션은 autoIncrement) ✅ |
| `npx tsc --noEmit` | 통과 ✅ |
| `npx expo-doctor` | 20/20 ✅ |

**AAB 빌드 명령**(Expo 로그인 필요 → 오너 실행):
```bash
eas build -p android --profile production
```
→ 완료 후 EAS 대시보드 또는 `eas build:list`에서 AAB 다운로드 → Console 업로드.
> 나(앱팀)는 이 환경에서 빌드 불가(JDK/SDK 없음, EAS 인증 필요). **빌드는 오너/CTO 실행.**

---

## 2. 트랙 선택 — 콘솔 결과에 따라

### 경우 A: 조직 계정 또는 2023-11-13 이전 개인 계정
→ **12명/14일 요건 없음.** 내부 테스트로 검증 후 **바로 프로덕션 제출 가능**(심사 소요는 별도, 신규앱 수일~).

### 경우 B: 2023-11-13 이후 개인 계정
→ **프로덕션 액세스 전 비공개 테스트 12명·14일 필수.** 오픈일에 프로덕션이 못 맞을 수 있음(산술).
→ **오픈 = 웹 먼저 + 앱은 내부 테스트 체험**, 프로덕션은 14일 충족 후 전환.

**어느 경우든 지금 할 일: 내부 테스트 배포(§3) + 비공개 테스트 착수 준비(§4).**

---

## 3. 내부 테스트(Internal testing) 배포 절차 — 즉시 가능(요건 없음)
> 12명/14일 요건 **없음**. 최대 100명. 링크로 초대. **콘솔 결과와 무관하게 지금 실행 가능.**

1. Play Console → 앱 선택 → **테스트 → 내부 테스트**
2. **새 버전 만들기** → §1 명령으로 만든 **AAB 업로드**
3. 출시명(예: `1.0.0 (6)`) + 출시 노트([PLAY_STORE_RELEASE.md](PLAY_STORE_RELEASE.md) §9-2)
4. **테스터 탭 → 이메일 목록 생성** → 테스터 Google 이메일 추가
5. **검토 → 내부 테스트 출시** → 생성된 **초대 링크(opt-in URL)**를 테스터에게 전달
6. 테스터가 링크 열기 → "테스터 되기" → Play에서 설치
> 내부 테스트도 앱 콘텐츠(개인정보처리방침·Data Safety·Content Rating 등) 일부는 미리 채워야 함(§5).

---

## 4. 🔴 비공개 테스트 12명 — 오너 요청서 (리드타임 최장, 지금 시작)

> **경우 B라면 이게 오픈→프로덕션 전환의 임계경로.** 사람을 모으는 일이라 가장 오래 걸린다.
> **경우 A여도 손해 없음**(내부 테스트 겸용 가능).

### 오너가 모아야 할 것
- **테스터 12명 이상** (안전하게 **15~20명 권장** — 중간에 빠지면 14일 시계가 흔들릴 수 있음)
- 각자 **Google 계정 이메일**(Gmail 또는 Google로 로그인되는 이메일) — Play 설치에 필요
- **14일 연속 옵트인 유지**: 테스터가 초대 링크로 등록한 상태를 14일간 유지해야 함
  - 앱은 설치했다가 지워도 되지만 **"테스터 등록"은 풀지 말 것**(옵트아웃 금지)
  - 인원이 12명 아래로 떨어지면 요건 카운트가 흔들림 → 버퍼 필요
- 가능하면 **실제로 한 번씩 열어보기**(Google이 형식적 테스트를 걸러냄 — 진짜 사용 흔적이 유리)

### 누구로 채우나 (오너 결정)
- 오너 지인 · 업계 관계자(설계사/GA) · 팀 내부 · 가족 등 **Google 계정 있는 아무나 12~20명**
- ⚠️ **심사용 테스트 계정과는 별개.** 이건 "테스터", 그건 "심사자 로그인 계정"(§5).

### 진행 방식
1. 오너가 12~20명 **이메일 수집**
2. §3의 내부 테스트 또는 **비공개 테스트(Closed) 트랙**에 이메일 등록 + 초대 링크 전달
   - ※ **프로덕션 액세스용 14일 카운트는 "비공개(Closed) 테스트" 기준.** 내부(Internal)와 별개 트랙일 수 있으니, 경우 B면 **Closed 트랙으로 14일을 돌린다.**
3. 12명↑ 옵트인 + 14일 경과 → **프로덕션 액세스 신청** → Google 검토

---

## 5. 심사 제출 폼 — 준비된 답변(제출만 하면 됨)
> 답변 초안은 전부 [PLAY_STORE_RELEASE.md](PLAY_STORE_RELEASE.md)에 있음. Console에 그대로 입력.

| 폼 | 위치 | 상태 |
|---|---|---|
| Data Safety | PLAY_STORE_RELEASE §3 | 답변 완비 → Console 입력·제출 |
| Content Rating(IARC) | §4 | 답변 완비 → 설문 제출 |
| 개인정보처리방침 URL | `https://bohummap.com/privacy` | 배포됨 ✅ |
| 데이터 삭제 URL | 웹 `/delete-account`(확인 필요) | 오너 확인 |
| App access(심사용 계정) | §9-1 템플릿 | **오너가 계정 생성 필요**(아래) |
| assetlinks.json | `web-handoff/well-known/` | 웹 배포 + CTO SHA-256 |

### 심사용 테스트 계정 (오너 생성)
- **bohummap.com 이메일 인증 회원 1개** (이메일 + 비밀번호)
- **오너 개인 계정(ohys1102) 말 것**, **2단계 인증/OTP 없이**
- 채팅·등록 등 게이트 기능 시연 가능한 상태
- 카카오/구글은 탐색전용 → 심사자는 이메일 계정 사용

---

## 6. 아직 없는 에셋 (디자인/오너)
- ❌ **Feature Graphic 1024×500** (디자인 제작)
- ❌ **스크린샷 2~8장** (홈/지도검색/지점상세/채팅/프로필 권장)
- ✅ 스토어 아이콘 512 (`assets/icon-512-store-v2.png` 확보)

---

## 요약 — 지금 병렬로 굴릴 것
```
[오너]  콘솔에서 계정유형·프로덕션액세스 상태 확인  ← 경우 A/B 결정
[오너]  비공개 테스트 12~20명 이메일 수집(리드타임 최장) ← 지금 시작
[오너]  심사용 테스트 계정 1개 생성
[오너/CTO]  eas build production → AAB
[디자인]  Feature Graphic + 스크린샷
[웹]    assetlinks.json 배포(+CTO SHA), /delete-account 확인
[앱팀]  준비/검증 완료(빌드 green, 문서). 콘솔 결과 오면 트랙 확정 지원
```
