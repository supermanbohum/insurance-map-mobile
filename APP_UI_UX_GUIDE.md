# 보험맵 앱 UI/UX 가이드 (APP_UI_UX_GUIDE)

> 작성일: 2026-08-04
> 목표: **보험맵 앱만의 일관되고 신뢰감 있는 디자인 시스템.** 웹보다 완성도 높은 모바일 경험.
> 관련 문서: [MOBILE_APP_MASTER_PLAN.md](MOBILE_APP_MASTER_PLAN.md) · [APP_FEATURE_ROADMAP.md](APP_FEATURE_ROADMAP.md)
> 이 가이드는 **네이티브 UI 레이어(스플래시/온보딩/오버레이/Phase 2+ 네이티브 화면)**에 적용된다. WebView 안 웹 화면은 웹팀 디자인을 따른다.

---

## 1. 디자인 원칙 (Design Principles)

1. **신뢰감 우선** — 금융/보험 도메인. 화려함보다 안정감. 절제된 네이비 + 넉넉한 여백.
2. **빠른 체감** — 모든 인터랙션에 100ms 내 시각/촉각 피드백. 로딩은 스켈레톤으로.
3. **한 손 조작** — 주요 액션은 화면 하단(엄지 영역). Bottom Nav + FAB.
4. **일관성** — 같은 요소는 어디서나 같게. 토큰 기반 스타일.
5. **접근성** — 최소 터치 타깃 44×44pt, 대비 WCAG AA, 동적 폰트 대응.

---

## 2. 컬러 (Color System)

### 2.1 브랜드 컬러 (코드 실측 기준)

앱의 Primary는 **`#2472EC`(브랜드 블루)**로 확정한다(B1, 2026-08-08 디자인 결정). 스플래시·
아이콘·런처 아이콘 배경과 동일 색이라 **아이콘 탭→스플래시→온보딩→로딩바까지 색이 끊기지 않는다.**
구 네이비 `#152D70`은 Primary에서 물러나 **텍스트·다크 서피스(ink) 용도**로 지위 변경(삭제하지 않음).

```
Brand Blue (Primary)   #2472EC   ███  주요 버튼/헤더/브랜드/스플래시/아이콘
Blue Pressed           #1B57D9   ███  Primary pressed (동일 색상 10% 어둡게)
Ink (구 Navy)          #152D70   ███  텍스트/다크 서피스 (Primary 아님)
```

### 2.2 팔레트 토큰

| 토큰 | Light | 용도 |
|---|---|---|
| `--color-primary` | `#2472EC` | 주요 액션, 브랜드 |
| `--color-primary-pressed` | `#1B57D9` | 눌림 상태 |
| `--color-ink` | `#152D70` | 텍스트/다크 서피스(구 네이비) |
| `--color-accent` | `#3B82F6` | 보조 강조(정보) |
| `--color-success` | `#16A34A` | 성공/승인 |
| `--color-warning` | `#F59E0B` | 주의 |
| `--color-danger` | `#B91C1C` | 오류/삭제 (오프라인 배너와 통일) |
| `--color-bg` | `#FFFFFF` | 화면 배경 |
| `--color-surface` | `#F7F8FA` | 카드/시트 배경 |
| `--color-surface-alt` | `#EEF1F6` | 구분 영역 |
| `--color-border` | `#E5E7EB` | 경계선 |
| `--color-text` | `#111827` | 본문 |
| `--color-text-sub` | `#6B7280` | 보조 텍스트 |
| `--color-text-disabled` | `#9CA3AF` | 비활성 |
| `--color-overlay` | `rgba(17,24,39,0.5)` | 모달 딤 |

### 2.3 다크 모드 (v2.0)

| 토큰 | Dark |
|---|---|
| `--color-bg` | `#0B1020` |
| `--color-surface` | `#151B2E` |
| `--color-text` | `#F3F4F6` |
| `--color-text-sub` | `#9CA3AF` |
| `--color-primary` | `#4C6EF5` (어두운 배경 대비 상향) |

> 명세 색상은 모두 토큰(`constants/colors.ts`)로 관리하고 하드코딩 금지.

---

## 3. 타이포그래피 (Typography)

시스템 폰트 사용(iOS SF / Android Roboto). 한글은 시스템 기본(Apple SD Gothic / Roboto→본고딕).

| 스타일 | 크기/굵기 | 용도 |
|---|---|---|
| Display | 28 / 800 | 스플래시, 온보딩 타이틀 |
| H1 | 22 / 700 | 화면 제목 |
| H2 | 18 / 700 | 섹션 제목 |
| Body | 15 / 400 | 본문 |
| Body Strong | 15 / 600 | 강조 본문 |
| Caption | 13 / 500 | 보조/라벨 |
| Micro | 11 / 500 | 뱃지/타임스탬프 |

- 행간: 본문 1.4, 제목 1.25
- 최대 폭: 긴 텍스트는 좌우 20pt 여백

---

## 4. 간격 & 형태 (Spacing / Radius / Elevation)

### 4.1 스페이싱 스케일 (4pt 그리드)
```
xs 4 · sm 8 · md 12 · lg 16 · xl 20 · 2xl 24 · 3xl 32
```
- 화면 좌우 기본 패딩: `xl(20)`
- 카드 내부 패딩: `lg(16)`
- 리스트 아이템 간격: `md(12)`

### 4.2 라운드(Radius)
```
sm 8 (칩/작은버튼) · md 12 (버튼/입력) · lg 16 (카드) · xl 20 (시트) · full 999 (FAB/아바타)
```

### 4.3 그림자(Elevation) — 절제
```
e1 카드:    y2  blur8  rgba(17,24,39,0.06)
e2 시트/팝: y8  blur24 rgba(17,24,39,0.12)
e3 FAB:    y6  blur16 rgba(21,45,112,0.24)
```
> 금융 앱은 과한 그림자를 피한다. 경계선(border)과 배경 대비로 구분 우선.

---

## 5. 버튼 (Buttons)

### 5.1 종류

| 종류 | 배경 | 텍스트 | 용도 |
|---|---|---|---|
| **Primary** | `--color-primary` | 흰색 | 화면당 1개 주요 액션 |
| **Secondary** | `--color-surface` + border | primary | 보조 액션 |
| **Ghost** | 투명 | primary | 3순위/취소 |
| **Danger** | `--color-danger` | 흰색 | 삭제/차단 |
| **Text** | 없음 | primary-tint | 인라인 링크 |

### 5.2 사양
- 높이: `48pt`(기본), `40pt`(compact), `56pt`(전체폭 CTA)
- 라운드: `md(12)`
- 최소 터치 타깃 44pt 보장
- **상태**: default / pressed(scale 0.98 + primary-pressed) / disabled(opacity, text-disabled) / loading(스피너, 라벨 유지, 재탭 방지)
- **햅틱**: 탭 시 `light`, 성공 액션 완료 시 `success`, 오류 시 `error` (브릿지 `haptic` 재사용)

### 5.3 규칙
- 한 화면에 Primary는 하나. 나머지는 Secondary/Ghost.
- 파괴적 액션은 Danger + 확인 다이얼로그.

---

## 6. 카드 (Cards)

보험맵의 핵심 리스트 단위 = **설계사/지점/GA 카드**.

### 6.1 기본 카드
```
┌─────────────────────────────────────┐
│ [아바타]  이름 (H2)          [뱃지]   │   ← 상단: 아이덴티티
│           소속 GA · 지역 (Caption)    │
│ ─────────────────────────────────── │
│  경력 5년 · 리뷰 4.8 · TOP           │   ← 메타 칩 라인
│ ─────────────────────────────────── │
│  [전화]   [채팅]   [공유]   [♡]      │   ← 하단: 액션 바
└─────────────────────────────────────┘
```
- 배경 `--color-surface`, 라운드 `lg(16)`, 그림자 `e1`, 패딩 `lg(16)`
- 탭 시 전체 카드가 눌림(scale 0.99) + `light` 햅틱 → 상세로 전환
- 액션 아이콘은 개별 터치 타깃 44pt

### 6.2 변형
- **TOP 설계사 카드**: 상단에 골드 그라데이션 리본 + 순위 뱃지
- **리크루팅 카드**: 좌측 컬러 바(accent), "제안" CTA 강조
- **스켈레톤 카드**: 로딩 시 shimmer(회색 블록 애니메이션)

### 6.3 칩(Chip)
- 높이 28, 라운드 `full`, 배경 `surface-alt`, 텍스트 Caption
- 선택형 칩: 선택 시 primary 배경 + 흰 텍스트

---

## 7. 애니메이션 (Animation)

### 7.1 원칙
- **의미 있는 모션만.** 방향/인과를 설명하는 움직임.
- 기본 이징: `ease-out`(진입), `ease-in`(퇴장). 스프링은 상호작용 요소(FAB, 시트).
- 지속시간: 마이크로 120ms · 표준 240ms · 화면전환 300ms · 강조 400ms
- `react-native-reanimated`(SDK 57 호환) 사용, UI 스레드에서 60fps 유지.

### 7.2 표준 모션

| 상황 | 모션 |
|---|---|
| 버튼 탭 | scale 1→0.98, 120ms |
| 카드 등장 | fade+상승 8pt, 스태거(순차 40ms) |
| 스플래시→앱 | 로고 축소 + 크로스페이드 (기존 AnimatedSplash 유지) |
| 모달/시트 | 하단에서 슬라이드 업 + 배경 딤 페이드 |
| 토스트/스낵바 | 하단에서 8pt 상승 + 페이드, 2.5초 후 자동 |
| 리스트 새로고침 | pull-to-refresh 스피너(브랜드 네이비) |
| 좋아요(♡) | 하트 팝(scale 1→1.3→1) + `success` 햅틱 |
| 스켈레톤 | shimmer 좌→우 1.2s 루프 |

### 7.3 금지
- 과도한 바운스, 3초 넘는 로딩 연출, 사용자 대기를 늘리는 장식 모션.

---

## 8. 제스처 (Gestures)

| 제스처 | 동작 |
|---|---|
| **좌측 엣지 스와이프** | 뒤로가기(iOS 표준) / 안드로이드는 시스템 백과 조화 |
| **하드웨어 백(Android)** | WebView 히스토리 → "한 번 더 누르면 종료" (기존 구현 유지) |
| **Pull-to-refresh** | WebView/리스트 새로고침 (기존 `pullToRefreshEnabled` 유지) |
| **시트 드래그다운** | 바텀시트 닫기 |
| **롱프레스(카드)** | 빠른 액션 메뉴(공유/저장/신고) + `medium` 햅틱 |
| **스와이프(채팅 목록)** | 좌스와이프 → 읽음/보관 액션 |
| **더블탭(프로필 이미지)** | 좋아요/저장 |

- 모든 제스처는 시각/햅틱 피드백 동반.
- 제스처는 항상 버튼으로도 가능해야 함(발견 가능성 보장).

---

## 9. 화면 전환 (Screen Transitions)

| 전환 유형 | 애니메이션 | 사용처 |
|---|---|---|
| Push(계층 진입) | 우→좌 슬라이드 | 목록 → 상세 |
| Modal(맥락 이탈) | 하→상 슬라이드 + 딤 | 등록/문의/QR |
| Sheet(부분 표시) | 바텀시트 스냅 포인트 | 필터/공유/액션 |
| Tab 전환 | 크로스페이드(무방향) | Bottom Nav 탭 |
| Deeplink 진입 | 페이드인 + 대상 하이라이트 | 알림/공유 링크 |

- WebView ↔ 네이티브 화면 전환 시 **흰 플래시 방지**: 전환 중 브랜드 배경 유지. **브랜드 배경색은 `#2472EC`**로 통일(B1, 스플래시·아이콘·런처 아이콘·Primary 동일 → 아이콘 탭→스플래시→온보딩→로딩바 색 연속). 구 네이비 `#152D70`은 ink(텍스트/다크 서피스)로 지위 변경.
- 전환 중에도 상태바 스타일 일관성(`set-status-bar` 브릿지 활용).

---

## 10. Bottom Navigation (v2.0)

```
┌──────────────────────────────────────────────┐
│   🏠        🔍        💬        🔔       👤     │
│  홈       검색      채팅③     알림②    내정보   │
└──────────────────────────────────────────────┘
```

- **5개 탭**: 홈 · 검색 · 채팅 · 알림 · 내정보
- 높이 56pt + Safe Area 하단 inset(기존 insets.bottom 처리 방식과 통일)
- 배경 `--color-bg` + 상단 hairline border, 살짝의 `e2` 상단 그림자
- 활성 탭: primary 아이콘/라벨 + 상단 인디케이터(2pt), 비활성: text-sub
- 탭 전환 시 `selection` 햅틱
- **뱃지**: 채팅/알림 미읽음 수(빨강 원 + 흰 숫자, 99+ 처리)
- 탭 재탭 시 해당 탭 스크롤 최상단 이동
- 초기(v1.x)에는 WebView가 각 탭 콘텐츠를 담당, v2.0에서 점진 네이티브화

---

## 11. FAB (Floating Action Button)

- 위치: 우측 하단, Bottom Nav 위 `16pt`, 우측 `20pt`
- 크기: 56×56, 라운드 `full`, 배경 primary, 그림자 `e3`
- 아이콘: 화면 맥락별 주요 액션 (예: 검색 화면 → 지도 토글, 프로필 → QR 공유)
- **Speed-dial(확장형)**: 탭 시 2~3개 서브 액션이 위로 스태거 등장
  - 예: `＋` → [설계사 등록] [지점 등록] [문의하기]
- 스크롤 다운 시 축소/숨김, 스크롤 업 시 재등장
- 탭 시 `light` 햅틱 + scale 스프링

---

## 12. 알림 UX (Notification UX)

### 12.1 권한 요청(프리퍼미션)
- OS 권한 팝업을 바로 띄우지 않는다. **먼저 앱 내 설명 화면**으로 가치를 설명("새 채팅·리크루팅 제안을 놓치지 마세요") → 동의 시 OS 팝업.
- 거부해도 앱은 정상 동작. 설정에서 재요청 가능.

### 12.2 포그라운드 알림
- 앱 사용 중 알림 도착 → **상단 인앱 배너**(3초, 탭 시 이동, 스와이프로 닫기)
- 현재 보고 있는 채팅방의 메시지는 배너 억제(중복 방지)

### 12.3 알림 종류별 스타일
| 종류 | 아이콘/색 | 액션 |
|---|---|---|
| 채팅 | 💬 primary | 탭 → 해당 방 |
| 리크루팅 | 🤝 accent | 탭 → 제안 상세 |
| 승인(광고/지점) | ✅ success | 탭 → 대상 |
| 공지 | 📢 text-sub | 탭 → 공지 |

### 12.4 알림 센터(v1.5)
- 받은 알림 타임라인, 읽음/안읽음 구분, 종류별 필터
- 좌스와이프 삭제, 상단 "모두 읽음"

---

## 13. 채팅 UX (Chat UX)

> v1.x는 WebView 채팅 + 푸시. v2.0에서 네이티브 채팅으로 전환. 아래는 네이티브 목표 사양.

### 13.1 레이아웃
```
┌─────────────────────────────────────┐
│ ‹  홍길동 · 삼성생명 GA        ⋮      │  ← 헤더(뒤로/상대/메뉴)
├─────────────────────────────────────┤
│                       [내 말풍선]     │  navy 배경, 흰 텍스트, 우측
│  [상대 말풍선]                        │  surface 배경, 좌측
│                       읽음 · 14:32    │  Micro, text-sub
│           ─── 오늘 ───                │  날짜 구분선
├─────────────────────────────────────┤
│ [＋] [ 메시지 입력…        ] [전송]   │  입력 바(Safe Area 고정)
└─────────────────────────────────────┘
```

### 13.2 말풍선
- 내 메시지: primary 배경 / 흰 텍스트 / 우측 정렬 / 라운드(좌하 제외 큰 라운드)
- 상대: surface 배경 / text / 좌측 / 아바타
- 연속 메시지는 아바타/시간 묶음(그룹핑)
- 상태: 전송중(반투명)→전송됨(체크)→읽음(파란 체크)

### 13.3 입력 바
- 하단 고정, 키보드 등장 시 부드럽게 밀림(`KeyboardAvoidingView` / reanimated)
- `＋`: 사진/카메라/파일/명함QR 첨부 시트
- 전송 버튼: 텍스트 있을 때만 primary 활성
- 입력 중 `selection` 햅틱 최소화(과하지 않게)

### 13.4 인터랙션
- 새 메시지 도착: 하단 부드러운 등장 + 스크롤 팔로우(내가 하단에 있을 때만)
- 위로 스크롤 중 새 메시지: 하단 "새 메시지 ↓" 플로팅 칩
- 말풍선 롱프레스: 복사/답장/삭제 + `medium` 햅틱
- 이미지 탭: 풀스크린 뷰어(핀치 줌)
- 타이핑 인디케이터(상대 입력 중 … 애니메이션)
- 전송 성공 `light`, 실패 `error` 햅틱

### 13.5 성능
- `FlashList`(가상화), 이미지 지연 로딩/캐시
- Supabase Realtime 구독(네이티브 전환 시), 재연결 자동 처리
- 오프라인 시 전송 대기 큐 + 재연결 자동 전송

---

## 14. 구현 노트 (Implementation Notes)

- 모든 디자인 값은 **토큰 파일**로: `src/config/theme.ts` (colors/spacing/radius/typography/elevation)
- 컴포넌트는 `src/components/ui/`에: `Button`, `Card`, `Chip`, `Sheet`, `Toast`, `Skeleton`, `Avatar`, `Badge`
- 모션은 `react-native-reanimated`(SDK 57 호환 버전), 햅틱은 기존 `expo-haptics` 브릿지 재사용
- Safe Area는 기존 `react-native-safe-area-context` 패턴 유지(상/하단 inset 처리)
- 접근성: `accessibilityLabel`/`accessibilityRole` 필수, 최소 대비/터치 타깃 준수
- **웹 화면(WebView 내부)은 이 가이드를 강제하지 않는다** — 네이티브 레이어와 색/톤만 이질감 없게 맞춘다(브랜드 네이비 공통).

> 코드 작성 전, `react-native-reanimated` 등 신규 모듈은 반드시 https://docs.expo.dev/versions/v57.0.0/ 에서 SDK 57 호환성을 확인하고 `npx expo install`로 설치한다 (AGENTS.md 규칙).
